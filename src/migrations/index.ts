import { Init1790317777977 } from './1790317777977-Init.js';

/**
 * 적용할 마이그레이션 목록 (순서대로).
 * `npm run migration:generate -- src/migrations/<이름>`으로 만든 뒤 여기에 추가한다.
 */
export const migrations = [Init1790317777977];
